{
  "name": "sqlitedeploy-cloudflare-workers-readonly",
  "version": "0.0.1",
  "private": true,
  "description": "Cloudflare Worker reading from a sqlitedeploy primary over Hrana HTTP — proof point for sqlitedeploy v2 edge support.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.86.0"
  }
}
